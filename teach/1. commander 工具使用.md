```js
#!/usr/bin/env node
// console.log("大喵课堂 damiao classroom. damiao");
/*
    porcess.argv return Array
    // 第一个参数：node.js可执行文的绝对路径
    // 第二个参数：正在执行的Javascript文件路径
    // 从从第三个参数开始：就是我们通过命令行传递进入的参数
*/

// process.argv.forEach(v => {
//     console.log(v); // 2
// });
const { program } = require('commander');

program
    .version("0.0.2")
    .usage('<command> [damiao options]')
    .option('-cr, --classroom <custom classroom\'s name>','current classroom name','damiao classroom')
    .command('add','add a damiao template')
    .command('init','initilize a damiao project')
    .command('create [options] <app-name>','create a new project powered by vue-cli-service')
    .parse(process.argv);

    console.log(program.classroom)
```