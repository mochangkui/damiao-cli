```js
#!/usr/bin/env node
const download = require('download-git-repo');

// donwload(仓库地址, 本地存放地址, 选项配置, 回调函数)

download("vuejs/awesome-vue","project/awesome-vue",(err) => {
    if(err){
        console.log(err)
    }else{
        console.log('success');
    }
});
```