```js
#!/usr/bin/env node
const chalk = require('chalk');
const log = console.log

// log(chalk.red('damiao classroom'));
// log(chalk.yellow('damiao classroom'));
// log(chalk.green('damiao classroom'));
// log(chalk.white('damiao classroom'));
// log(chalk.grey('damiao classroom'));

// log(chalk.bgRed('damiao classroom'));
// log(chalk.bgYellow('damiao classroom'));
// log(chalk.bgGreen('damiao classroom'));
// log(chalk.bgBlue('damiao classroom'));

log(chalk`
    大家好，我是大喵，欢迎来到{yellow.bold 大喵课堂}。
    weclome to {green damiao classroom}.
`);
```