```js
#!/usr/bin/env node

const inquirer = require('inquirer');

// 定义基本的问答结构

// 1. 定义问题列表
/*const promptList = [
    {
        type: 'input',
        message: '我们来设置一个用户名',
        name: 'username',
        default: 'damiao'
    },
    {
        type: 'input',
        message: '请输入6位数字的ID',
        name: 'id',
        validate: (val) => {
            if(val.match(/^\d{6}$/ig)){
                return true;
            }
            return "请输入6位数字的ID";
        }
    }
];*/

// 定义 是/否 选择问题列表
/*const promptList = [
    {
        type: 'confirm',
        message: '是否使用监听模式？',
        name: 'watch',
        prefix: "🌹"
    },
    {
        type: 'confirm',
        message: '是否进行文件的批量监听？',
        name: 'more-watch',
        suffix: '👩‍❤️‍👩',
        when: (answers) => {
            if(answers.watch){
                return true;
            }else{
                return false;
            }
        }
    }
];*/

// 定义 问题列表 单选模式
/*const promptList = [
    {
        type: 'list',
        message: '请选择一种单页面前端技术？',
        name: 'technology',
        choices: [
            'Vue','React','Angular'
        ]
    }
];*/

// 定义 问题答案列表 多选模式
/*const promptList = [
    {
        type: 'checkbox',
        message: '选择大喵',
        name: 'damiao',
        choices: [
            "大喵1号", "大喵2号", "大喵3号", "大喵4号"
        ],
        pageSize: 2
    }
];*/

// 定义 类型
const promptList = [
    {
        type:"password",
        message: '请输入密码：',
        name: 'pwd'
    },
    {
        type: 'editor',
        message: '请输入备注文本：',
        name:'editor-content'
    }
];


// 2. 获取问题回答答案
inquirer.prompt(promptList).then(answers => {
    console.log(answers);
})
```